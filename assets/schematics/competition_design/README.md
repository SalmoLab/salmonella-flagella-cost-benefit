# Competition-design schematic source

`competition_scheme_source.pptx` is an immutable copy of the editable collaborator source (SHA-256 `fc70af99e68f06eba7a122121987f1024cdb34188bf6cb232ca79321409d56a9`). The canonical Python reconstruction reads the slide text directly, verifies the four region labels, and redraws the experimental logic as editable vector geometry.

The source spells the gene as `fhlDC`; the canonical schematic corrects this transposition to `flhDC` and records the normalization in the panel-D source-data table.
