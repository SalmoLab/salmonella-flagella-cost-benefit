# Final cell-economy model intake target

This directory intentionally contains no reconstructed model code. The current
Figure 2G-H, Figure 3C and Supplementary Figure S3 model source is pending from
the collaborator. The local 2024 summaries and stochastic flagella simulation
are preliminary or different models and are not canonical substitutes.

On receipt, preserve the package unchanged under `archive/incoming/`, then place
the verified source snapshot, licence, parameter tables, solver configuration and
environment lock here. Canonical scripts must reproduce the targets in
`config/validation_targets.csv` before these panels are unblocked.

