# Final gradient model intake target

The final source for Figure 3E-G is pending from the collaborator. This directory
must ultimately contain the gradient geometry/equations, initial and boundary
conditions, movement mapping, uptake/growth/biomass equations, deterministic or
stochastic settings, seeds, solver configuration and full time-series outputs.

No curve digitization or inferred implementation is accepted. The final package
must reproduce the six normalized biomass targets and the unique 3% optimum
recorded in `config/validation_targets.csv`.
