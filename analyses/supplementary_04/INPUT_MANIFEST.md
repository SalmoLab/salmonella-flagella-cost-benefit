# Supplementary Figure 5 inputs

| Canonical input | Rows | Origin |
|---|---:|---|
| `models/motility_simulation/upstream/data/motility_summary_parameters.csv` | 8 | Exact collaborator/GitHub parameter table at commit `96ca0e7` |
| `models/motility_simulation/upstream/data/config.yml` | — | Exact collaborator/GitHub simulation configuration at commit `96ca0e7` |

The complete vendored source and its hashes are in `models/motility_simulation/upstream/`. The former 208-row legacy summary table is retained as historical data but is no longer an input to S5.
