# Supplementary Figure 1 — normalized single-cell growth

Current Supplementary Figure 1 is reconstructed from `growth_norm` in the same
processed table as main Figure 2C. The separately named legacy supplementary
bundle is stale/historical and is deliberately not a canonical input.

- Panel A: `mother_only` and `non_mother`, pooled 200–800 min.
- Panel B: `mother_only` and `non_mother`, pooled 180–480 min (3–8 h).

Both panels are partial reproductions because the raw mother-machine images,
tracking files and normalization pipeline are absent.
