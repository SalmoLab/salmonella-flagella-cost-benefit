The collaborator package passed downstream numerical validation on 12 August
2026. Exact targets are recorded in `config/validation_targets.csv`; generated
outputs live only under `build/panels/S2_A/`. The frozen July figure remains the
visual reference.
