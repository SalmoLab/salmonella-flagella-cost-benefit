# F1_F — blocked schematic asset

Scientific purpose: define the `Ppro1`, `PproA`, `PproB` and `PproD` synthetic `flhDC` promoter series and its expression ordering. The July reference is `reference/2026-07-09/figures/Figure_1.svg`, but no editable canonical source, construct-to-strain mapping, linked artwork, font/licence record or deterministic export recipe has been identified. There are no quantitative data, replicates, transformations or statistical tests for this schematic panel.

`config/asset_intake.json` lists the required inputs and acceptance tests; `metadata/provenance.json` records the blocked state and frozen-reference checksum. `scripts/` deliberately contains no build, and `expected/` contains no approved regression artifact. No substitute asset has been created or extracted from the assembled figure.
