# F1_G — blocked microscopy asset

Scientific purpose: show representative WT and `Ppro1/PproA/PproB/PproD-flhDC` cell and hook phenotypes underlying panel H. The July reference is `reference/2026-07-09/figures/Figure_1.svg`, but the exact raw phase/hook-fluorescence fields, biological-replicate/FOV identifiers, acquisition settings, selection rationale, registration and image-processing operations, exclusions and scale calibration have not been mapped. No inferential statistic belongs to this representative-image panel; image integrity and selection provenance are nevertheless required.

`config/asset_intake.json` lists the required raw assets, metadata and integrity checks; `metadata/provenance.json` records the blocked state and frozen-reference checksum. `scripts/` deliberately contains no build, and `expected/` contains no approved regression artifact. No image has been guessed or extracted from the assembled figure.
