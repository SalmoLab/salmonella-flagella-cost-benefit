# F2_D — blocked flagellar assembly-mutant schematic

Scientific purpose: current Figure 2D defines the structural interpretation of
the strains compared in Figure 2E–F: fully assembled flagellum, `ΔflgE`,
`ΔflgM flhAΔc`, `ΔflgKL`, and `motB(D33N)`. The panel labels filament (FliC),
hook-filament junction (FlgKL), hook (FlgE), motor (MotAB), and basal body.

The frozen July reference is
`reference/2026-07-09/figures/Figure_2.svg` (SHA-256
`5d0d4848c6f147f9cfd729c10c62b7c3e6f865edfaa687d29fe311fb03c14ea8`).
It is visual evidence only, not an editable or independently rebuildable source.
No schematic has been guessed, traced, or extracted from that composite.

`config/asset_intake.json` lists the required editable source, construct audit,
linked artwork, font/licence information, and deterministic export recipe.
There are no quantitative replicates, transformations, exclusions, statistical
tests, or multiplicity corrections for this explanatory schematic. The
scientific genotype-to-structure mapping must nevertheless be reviewed before
release because it conditions the interpretation of the adjacent experiments.

Status: `blocked_asset`. `scripts/` contains no build entry point and
`expected/` contains no approved panel target. Once the actual editable source
arrives, preserve it unchanged, checksum it, encode all text/placement/export
settings, and add a deterministic SVG/PDF/PNG producer plus visual regression.
