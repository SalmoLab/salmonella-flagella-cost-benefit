# Software versions

Generated by `tools/build_software_versions.py` from `uv.lock`, `requirements.lock`, `pyproject.toml` and `build/environment/bootstrap.json`. Do not edit by hand; run `make software-versions`.

## Language and platform

| Item | Value | Source |
|---|---|---|
| Python | 3.12.11 | build/environment/bootstrap.json |
| Python constraint | `==3.12.*` | uv.lock |
| Platform | macOS-26.6.1-arm64-arm-64bit | build/environment/bootstrap.json |
| Environment frozen | 2026-08-11T07:41:40.245255Z | build/environment/bootstrap.json |

## Packages

`Declared constraint` is what `pyproject.toml` asks for. `Version` is what `uv.lock` resolved. `Hash-pinned` states whether `requirements.lock` carries the artefact hashes of that version.

### Optimisation and solver stack

| Package | Version | Declared constraint | Hash-pinned |
|---|---|---|---|
| gekko | 1.3.2 | `==1.3.2` | yes |
| scipy | 1.18.0 | `>=1.14,<2` | yes |

### Numerical and tabular stack

| Package | Version | Declared constraint | Hash-pinned |
|---|---|---|---|
| numpy | 2.5.2 | `>=2.1,<3` | yes |
| pandas | 2.3.3 | `>=2.2,<3` | yes |
| pyarrow | 22.0.0 | `>=18,<23` | yes |
| scikit-learn | 1.9.0 | `>=1.6,<2` | yes |
| scikit-image | 0.25.2 | `>=0.25,<0.26` | yes |
| openpyxl | 3.1.5 | `>=3.1,<4` | yes |
| xlsxwriter | 3.2.9 | `>=3.2,<4` | yes |

### Plotting and image stack

| Package | Version | Declared constraint | Hash-pinned |
|---|---|---|---|
| matplotlib | 3.11.1 | `>=3.9,<4` | yes |
| seaborn | 0.13.2 | `>=0.13.2,<0.14` | yes |
| adjusttext | 1.4.0 | `>=1.3,<2` | yes |
| cmcrameri | 1.10 | `>=1.9,<2` | yes |
| colorspacious | 1.1.2 | `>=1.1.2,<2` | yes |
| pillow | 11.3.0 | `>=11,<12` | yes |
| imageio | 2.37.4 | `>=2.36,<3` | yes |
| cairosvg | 2.9.0 | `>=2.7,<3` | yes |

### Workflow and validation

| Package | Version | Declared constraint | Hash-pinned |
|---|---|---|---|
| snakemake | 9.25.1 | `>=9,<10` | yes |
| pyyaml | 6.0.3 | `>=6.0,<7` | yes |
| jsonschema | 4.26.0 | `>=4.23,<5` | yes |

### Testing and linting

| Package | Version | Declared constraint | Hash-pinned |
|---|---|---|---|
| pytest | 8.4.2 | `>=8.3,<9` | yes |
| pytest-regressions | 2.11.0 | `>=2.7,<3` | yes |
| ruff | 0.16.2 | `>=0.9,<1` | yes |

## Solver route

The cell-economy model is solved through GEKKO on a remote server, so no lock file can pin the solver. The route is recorded in `models/cell_economy/LOCAL_SOLVER_VALIDATION.md` and reproduced here.

| Item | Value |
|---|---|
| GEKKO client | 1.3.2 |
| Server | https://apmonitor.com |
| Solver | APMonitor solver 3, IPOPT v3.12, server-side |

## Remaining packages

`uv.lock` resolves 103 packages in total. The tables above name 23 of them, which are the packages that produce a number or a mark in a figure. The other 80 are transitive dependencies; every one is pinned by version and by hash in `requirements.lock`, and that file is the complete record.

## Software this repository does not pin

`rsvg-convert` (librsvg) rasterises the assembled figures and renders the Supplementary Information PDF, and Ghostscript joins its pages. Both are host programs outside the Python environment, so no lock file records them. `build/supplementary_information/Supplementary_Information.manifest.json` records the version of each one that produced the current PDF.

Image acquisition, segmentation and mass-spectrometry software is named in the manuscript Methods and is not installed here. See `docs/ENVIRONMENT.md`.
